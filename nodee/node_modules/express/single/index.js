const { path } = require("lib/application.js");

const route =async () => {
    const routes = [
        {path: "/", view: () => console.log("viewing Dashboard")},
        {path: "/posts", view: () => console.log("viewing posts")},
        {path: "/settings", view: () => console.log("viewing Settings")},
      ];
}